Barebones example of how to deploy a Q&A pipeline on Google cloud. 

Uses the Haystack framework for the ML part, the FastAPI library for app hosting (on Google Cloud Run). 

Can be started from by invoking python pipline.py with the arguments you see in the "main" function. 

Shoul set PROJECT_NAME=<your-gcp-project-name> as an environment variable a